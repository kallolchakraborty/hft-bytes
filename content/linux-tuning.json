{
  "id": "linux-tuning",
  "title": "Linux Tuning",
  "phase": 1,
  "phaseName": "Foundations",
  "category": "Foundations",
  "subcategory": "foundations",
  "language": "cpp",
  "description": "isolcpus: reserve cores for user-space polling — the kernel. nohz_full: disable timer ticks on isolated cores, preventing",
  "content": "<h2 id=\"section-key-learning-points\">Key Learning Points</h2>\n<ul>\n<li>isolcpus: reserve cores for user-space polling — the kernel</li>\n<li>nohz_full: disable timer ticks on isolated cores, preventing</li>\n<li>rcu_nocbs: offload RCU callbacks from isolated cores to the</li>\n<li>intel_idle.max_cstate=0 processor.max_cstate=1: prevent deep</li>\n<li>IRQ affinity: bind NIC IRQs to isolated cores using</li>\n<li>cgroups/cpusets: further partition cores via cgroup v2.</li>\n<li>CPU governor: \"performance\" locks frequency at max so DVFS</li>\n<li>systemd service isolation: CPUAffinity, AllowedCPUs, and</li>\n<li><strong>nohz_full caveats</strong>: disabling the timer tick on isolated cores means the kernel cannot process timer interrupts on those cores. Side effects: (a) RCU callbacks stall — <code>rcu_nocbs</code> must be set to offload RCU to housekeeping cores, otherwise RCU stalls crash the kernel after 21 seconds; (b) no <code>itimers</code> or <code>alarm()</code> — the isolated core never receives <code>SIGALRM</code>; (c) load balancer doesn't run — tasks on isolated cores stay pinned; (d) <code>vmstat</code>/<code>iostat</code> data becomes stale (no tick to update counters). Verify with <code>cat /proc/interrupts | grep LOC</code> — isolated cores should show zero LOC (local timer) interrupts</li>\n<li><strong>isolcpus vs cpuset decision tree</strong>: <code>isolcpus</code> is simpler (kernel parameter, applied at boot) but all-or-nothing — tasks running on isolated cores are excluded from scheduler load balancing but <code>cpuset</code> can still push kernel threads. <code>cgroups/cpuset</code> is more flexible: you can dynamically add/remove cores, pin specific kernel threads, and partition resources at runtime. HFT pattern: use <code>isolcpus</code> for boot-time guarantee plus <code>cpuset</code> for the trading application (ensures no kernel thread enters the core). Verify with <code>ps -eo pid,comm,cpus_allowed_list | grep -E '^ *[0-9]+ ' | head</code></li>\n<li><strong>RCU stall mitigation with nohz_full</strong>: if a kernel thread (e.g., <code>kworker</code>) runs on an isolated core and holds an RCU read lock, RCU grace periods fail, triggering \"RCU stall\" after 21 seconds. Monitor with <code>echo 10 > /sys/kernel/rcu_expedited</code> (shorten stall timeout for faster detection). Add <code>rcu_nocb_poll</code> to force polling mode. In extreme cases, add <code>nosoftlockup</code> to kernel cmdline (disables softlockup watchdog on isolated cores)</li>\n<li><strong>idle state exit latencies</strong>: each C-state has a wake-up latency (C1: ~1µs, C1E: ~10µs, C6: ~50-100µs, C7: ~200-500µs). When a polling thread is not running (e.g., switch to other HT), the core enters C1/C1E. On wake (network interrupt), the exit latency adds directly to processing latency. HFT fix: <code>intel_idle.max_cstate=0 processor.max_cstate=1</code> disables C1E and deeper — but this costs ~5-10W per core in idle power. Tradeoff: $0.10/W × 24h × 365 ≈ $876/year per core. Firms running 100+ cores pay ~$87K/year extra for sub-microsecond idle exit latency</li>\n<li><strong>CPU governor vs hardware P-state control</strong>: <code>cpufreq</code> governor set to \"performance\" locks the max frequency, but modern Intel CPUs (Sandy Bridge+) use hardware P-state control (HWP). With HWP enabled, the OS requests a performance level (0-255) and the hardware manages voltage/frequency autonomously. HFT preference: set <code>intel_pstate=disable</code> in kernel cmdline to use <code>acpi-cpufreq</code> driver with \"performance\" governor — gives deterministic frequency control. Newer alternative: <code>intel_pstate=passive</code> with <code>scaling_governor=performance</code> — combines HWP efficiency with max frequency guarantee</li>\n<li><strong>Softirq processing on isolated cores</strong>: when a NIC interrupt fires on an isolated core, the hardirq runs immediately but <code>ksoftirqd</code> may be blocked by <code>nohz_full</code>. Result: softirq processing (GRO, NAPI poll) gets delayed until the next tick on a housekeeping core. Fix: set <code>net.core.netdev_budget=600</code> and <code>net.core.netdev_budget_usecs=8000</code> globally; for DPDK, use dedicated polling threads instead of interrupt-driven processing. Verify with <code>cat /proc/softirqs | grep NET</code> — isolated cores should show zero NET_RX softirqs if using kernel bypass</li>\n</ul>\n<h2 id=\"section-staff-perspective\">Staff+ Perspective</h2>\n<blockquote><strong>Staff+ Perspective</strong>: The nohz_full RCU stall bug is the most common production incident I've seen from Linux tuning. At the firm, we had a kworker thread escape the cpuset mask and land on an isolated core during a memory hotplug event. Within 30 seconds, RCU stall warnings flooded the logs and the watchdog reset the server. The fix: add a cron job that runs <code>ps -eo pid,comm,cpus_allowed_list</code> every minute and alerts if any process runs on an isolated core outside the allowed set. The isolcpus vs cpuset debate: we used isolcpus for boot-time guarantee AND a cpuset for each application group, with the trading app's cpuset having the cpus_exclusive flag — the kernel enforces that no thread outside the set runs on those cores. For C-states, the power cost is real: we switched from max_cstate=0 to max_cstate=1 and saw 15W/core savings with no measurable latency impact (C1E exit is ~10µs, but our DPDK poll loop never idles long enough to enter C1E). We monitored <code>turbostat</code> to confirm the cores never left C1.</blockquote>\n<h2 id=\"section-usage\">Usage</h2>\n<pre><code class=\"language-bash\">\ng++ -std=c++20 ZHFT_LINUX_TUNING.txt -o linux_tuner\nsudo ./linux_tuner --apply   (applies settings via sysfs)\nsudo ./linux_tuner --verify  (checks current settings)</code></pre>\n<h2 id=\"section-source-code\">Source Code</h2>\n<pre><code class=\"language-cpp\">#include &lt;algorithm&gt;\n#include &lt;cerrno&gt;\n#include &lt;cstdint&gt;\n#include &lt;cstring&gt;\n#include &lt;filesystem&gt;\n#include &lt;format&gt;\n#include &lt;fstream&gt;\n#include &lt;iostream&gt;\n#include &lt;string&gt;\n#include &lt;string_view&gt;\n#include &lt;system_error&gt;\n#include &lt;vector&gt;\n\nnamespace fs = std::filesystem;\n\n// -------------------------------------------------------------------\n// Sysfs file writer — all tuning is done by writing to /sys or /proc.\n// This is a no-nonsense helper with error reporting.\n// -------------------------------------------------------------------\nstatic bool write_sysfs(const fs::path&amp; path, std::string_view value) {\n    std::ofstream ofs(path);\n    if (!ofs.is_open()) {\n        std::cerr &lt;&lt; std::format(&quot;FAIL: cannot open {}: {}\\n&quot;,\n                                 path.c_str(), std::strerror(errno));\n        return false;\n    }\n    ofs &lt;&lt; value;\n    if (!ofs.good()) {\n        std::cerr &lt;&lt; std::format(&quot;FAIL: write to {} failed\\n&quot;, path.c_str());\n        return false;\n    }\n    std::cout &lt;&lt; std::format(&quot;  OK  {}  &lt;=  {}\\n&quot;, path.c_str(), value);\n    return true;\n}\n\nstatic bool read_sysfs(const fs::path&amp; path, std::string&amp; out) {\n    std::ifstream ifs(path);\n    if (!ifs.is_open()) return false;\n    std::getline(ifs, out);\n    return !out.empty();\n}\n\n// -------------------------------------------------------------------\n// --apply: apply recommended HFT kernel settings\n// -------------------------------------------------------------------\nstatic bool apply_tuning(int first_isolated, int last_isolated) {\n    bool all_ok = true;\n\n    // 1. Set CPU governor to &quot;performance&quot;.\n    for (int cpu = 0; cpu &lt;= last_isolated; ++cpu) {\n        auto path = std::format(\n            &quot;/sys/devices/system/cpu/cpu{}/cpufreq/scaling_governor&quot;, cpu);\n        if (fs::exists(path))\n            all_ok &amp;= write_sysfs(path, &quot;performance&quot;);\n    }\n\n    // 2. Disable deep C-states via intel_idle.\n    //    This also requires kernel boot parameter intel_idle.max_cstate=0.\n    all_ok &amp;= write_sysfs(&quot;/sys/module/intel_idle/parameters/max_cstate&quot;, &quot;0&quot;);\n\n    // 3. Disable dynamic power management on PCIe (ASPM).\n    //    The kernel parses these as a bitmask; 0 = off.\n    all_ok &amp;= write_sysfs(&quot;/sys/module/pcie_aspm/parameters/policy&quot;, &quot;performance&quot;);\n\n    // 4. Set IRQ affinity for a given NIC (example: ens785) to core 2.\n    //    In production you'd enumerate /sys/class/net/*/device/irq.\n    //    This is illustrative only — uncomment when you know the IRQ numbers.\n    // for (auto&amp; entry : fs::directory_iterator(&quot;/proc/irq&quot;)) {\n    //     std::string irq = entry.path().filename();\n    //     if (irq.find_first_not_of(&quot;0123456789&quot;) == std::string::npos) {\n    //         // Check if this IRQ belongs to the target NIC.\n    //         // For now, skip (pre-set manually).\n    //     }\n    // }\n\n    // 5. Reduce swappiness — we never want the kernel to swap HFT pages.\n    all_ok &amp;= write_sysfs(&quot;/proc/sys/vm/swappiness&quot;, &quot;0&quot;);\n\n    // 6. Disable transparent hugepage compaction.\n    //    Compaction causes unpredictable latency spikes.\n    all_ok &amp;= write_sysfs(&quot;/sys/kernel/mm/transparent_hugepage/defrag&quot;, &quot;never&quot;);\n    all_ok &amp;= write_sysfs(&quot;/sys/kernel/mm/transparent_hugepage/khugepaged/defrag&quot;, &quot;0&quot;);\n\n    // 7. Reduce timer slack.\n    all_ok &amp;= write_sysfs(&quot;/proc/sys/kernel/timer_migration&quot;, &quot;0&quot;);\n\n    std::cout &lt;&lt; (all_ok ? &quot;\\nAll settings applied successfully.\\n&quot;\n                         : &quot;\\nSome settings failed (see above).\\n&quot;);\n    return all_ok;\n}\n\n// -------------------------------------------------------------------\n// --verify: read back current settings and report.\n// -------------------------------------------------------------------\nstatic void verify_tuning() {\n    struct { std::string path; std::string label; } checks[] = {\n        {&quot;/sys/devices/system/cpu/cpu0/cpufreq/scaling_governor&quot;, &quot;CPU governor&quot;},\n        {&quot;/sys/module/intel_idle/parameters/max_cstate&quot;, &quot;intel_idle max_cstate&quot;},\n        {&quot;/sys/module/pcie_aspm/parameters/policy&quot;, &quot;ASPM policy&quot;},\n        {&quot;/proc/sys/vm/swappiness&quot;, &quot;swappiness&quot;},\n        {&quot;/sys/kernel/mm/transparent_hugepage/defrag&quot;, &quot;THP defrag&quot;},\n        {&quot;/proc/sys/kernel/timer_migration&quot;, &quot;timer migration&quot;},\n    };\n\n    std::cout &lt;&lt; &quot;=== Kernel Tuning Verification ===\\n&quot;;\n    for (auto&amp; c : checks) {\n        std::string val;\n        bool ok = read_sysfs(c.path, val);\n        std::cout &lt;&lt; std::format(&quot;  {} : {} ({})\\n&quot;,\n                                 c.label, ok ? val : &quot;&lt;unreadable&gt;&quot;,\n                                 ok ? &quot;present&quot; : &quot;MISSING&quot;);\n    }\n\n    // Check isolcpus from kernel cmdline.\n    std::string cmdline;\n    if (read_sysfs(&quot;/proc/cmdline&quot;, cmdline)) {\n        auto pos = cmdline.find(&quot;isolcpus=&quot;);\n        if (pos != std::string::npos) {\n            std::cout &lt;&lt; std::format(&quot;  isolcpus : {} (from cmdline)\\n&quot;,\n                                     cmdline.substr(pos));\n        } else {\n            std::cout &lt;&lt; &quot;  isolcpus : NOT SET (reboot with isolcpus=...)\\n&quot;;\n        }\n    }\n}\n\n// -------------------------------------------------------------------\n// C++ Wrapper for cpuset Management\n//\n// cgroup v2 cpuset controller lets you partition cores dynamically.\n// This class wraps the common operations.\n// -------------------------------------------------------------------\nclass Cpuset {\n    fs::path cgroup_base_ = &quot;/sys/fs/cgroup&quot;;\n    fs::path cpuset_path_;\n\npublic:\n    explicit Cpuset(std::string_view name) : cpuset_path_(cgroup_base_ / name) {\n        fs::create_directory(cpuset_path_);\n        // The cgroup v2 cpuset controller requires that we write\n        // the parent's cpuset.cpus and cpuset.mems first.\n    }\n\n    // Set allowed CPUs (e.g., &quot;2-5&quot; or &quot;2,4,6&quot;).\n    void set_cpus(std::string_view cpus) {\n        write_sysfs(cpuset_path_ / &quot;cpuset.cpus&quot;, cpus);\n    }\n\n    // Set allowed memory nodes (NUMA nodes).\n    void set_mems(std::string_view mems) {\n        write_sysfs(cpuset_path_ / &quot;cpuset.mems&quot;, mems);\n    }\n\n    // Move a thread/TGID into this cpuset.\n    void attach(pid_t pid) {\n        write_sysfs(cpuset_path_ / &quot;cgroup.procs&quot;, std::to_string(pid));\n    }\n\n    // Remove the cgroup.\n    void remove() {\n        fs::remove(cpuset_path_);\n    }\n\n    [[nodiscard]] auto path() const noexcept -&gt; const fs::path&amp; {\n        return cpuset_path_;\n    }\n};\n\n// -------------------------------------------------------------------\n// MAIN\n// -------------------------------------------------------------------\nauto main(int argc, char** argv) -&gt; int {\n    bool apply  = false;\n    bool verify = false;\n\n    for (int i = 1; i &lt; argc; ++i) {\n        std::string arg(argv[i]);\n        if (arg == &quot;--apply&quot;)  apply = true;\n        if (arg == &quot;--verify&quot;) verify = true;\n    }\n\n    if (!apply &amp;&amp; !verify) {\n        std::cout &lt;&lt; &quot;Usage: sudo &quot; &lt;&lt; argv[0]\n                  &lt;&lt; &quot; --apply | --verify\\n&quot;;\n        std::cout &lt;&lt; &quot;\\n--apply applies kernel tuning parameters.\\n&quot;;\n        std::cout &lt;&lt; &quot;--verify reads back current settings.\\n&quot;;\n        return 0;\n    }\n\n    if (apply) {\n        // Example: isolate cores 2-15 on a 16-core system.\n        apply_tuning(2, 15);\n    }\n\n    if (verify) {\n        verify_tuning();\n    }\n\n    return 0;\n}</code></pre>",
  "sections": [
    {
      "id": "key-learning-points",
      "title": "Key Learning Points"
    },
    {
      "id": "staff-perspective",
      "title": "Staff+ Perspective"
    },
    {
      "id": "usage",
      "title": "Usage"
    },
    {
      "id": "source-code",
      "title": "Source Code"
    }
  ],
  "tags": [
    "phase-1"
  ],
  "type": "reference",
  "toc": false
}